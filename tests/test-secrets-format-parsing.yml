name: Test secrets format with multiple secrets and aliases
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Start mock Conjur server
        run: |
          python3 - << 'PYEOF' &
          import http.server, socketserver

          class ConjurHandler(http.server.BaseHTTPRequestHandler):
              def log_message(self, fmt, *args):
                  pass
              def do_POST(self):
                  n = int(self.headers.get('Content-Length', 0))
                  self.rfile.read(n)
                  body = b'ZmFrZXRva2Vu'
                  self.send_response(200)
                  self.send_header('Content-Type', 'text/plain')
                  self.send_header('Content-Length', str(len(body)))
                  self.end_headers()
                  self.wfile.write(body)
              def do_GET(self):
                  body = b'Malformed authorization token'
                  self.send_response(401)
                  self.send_header('Content-Type', 'text/plain')
                  self.send_header('Content-Length', str(len(body)))
                  self.end_headers()
                  self.wfile.write(body)

          socketserver.TCPServer.allow_reuse_address = True
          with socketserver.TCPServer(('0.0.0.0', 7778), ConjurHandler) as httpd:
              httpd.serve_forever()
          PYEOF
          echo $! > /tmp/mock_conjur_7778.pid
          sleep 2
          echo "Mock server started on port 7778"

      - name: Get Docker bridge host IP
        id: hostip
        run: |
          HOST_IP=$(ip route | awk '/default/ {print $3}' | head -1)
          if [ -z "$HOST_IP" ]; then
            HOST_IP="172.17.0.1"
          fi
          echo "host_ip=$HOST_IP" >> $GITHUB_OUTPUT
          echo "Host IP for Docker containers: $HOST_IP"

      - id: action
        uses: .
        continue-on-error: true
        with:
          url: "http://${{ steps.hostip.outputs.host_ip }}:7778"
          account: "myaccount"
          host_id: "host/github-action"
          api_key: "fake-api-key"
          # Multiple secrets with pipe-alias syntax and semicolon delimiter
          secrets: "db/username | DB_USER; db/password | DB_PASS; db/host"

      - name: Stop mock server
        if: always()
        run: |
          if [ -f /tmp/mock_conjur_7778.pid ]; then
            kill $(cat /tmp/mock_conjur_7778.pid) 2>/dev/null || true
          fi

      - name: Assert action failed at auth step not input parsing
        run: |
          echo "outcome: ${{ steps.action.outcome }}"
          # The action should fail due to "Malformed authorization token" error,
          # which means secrets format was parsed correctly (got past input parsing)
          # and failed at the secrets retrieval step.
          if [ "${{ steps.action.outcome }}" != "failure" ]; then
            echo "FAIL: expected action to fail when Conjur returns malformed token error"
            exit 1
          fi
          echo "PASS: action failed as expected (secrets format parsed, failed at retrieval)"
